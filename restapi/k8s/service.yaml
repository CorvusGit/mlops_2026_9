apiVersion: v1
kind: Service
metadata:
  name: fraud-api-service
  annotations:
    # Эта строка говорит Яндексу выделить IP из внутренней подсети
    yandex.cloud/load-balancer-type: internal
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8000
  selector:
    app: fraud-api