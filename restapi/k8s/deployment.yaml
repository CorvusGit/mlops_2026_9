apiVersion: apps/v1
kind: Deployment
metadata:
  name: fraud-api-deployment
  labels:
    app: fraud-api
spec:
  replicas: 1  # Для начала одного пода достаточно
  selector:
    matchLabels:
      app: fraud-api
  template:
    metadata:
      labels:
        app: fraud-api
    spec:
      containers:
      - name: fraud-api-container
        image: restapiv3:latest 
        imagePullPolicy: Never # для локальной разработки
        ports:
        - containerPort: 8000
        env:
        # Указываем адреса хостовой машины
        - name: MLFLOW_TRACKING_URI
          value: "http://192.168.88.251:5005"
        - name: KAFKA_BOOTSTRAP_SERVERS
          value: "192.168.88.251:9092"
        - name: KAFKA_TOPIC_OUT
          value: "predictions"
        resources:
          requests:
            memory: "2Gi"
            cpu: "500m"
          limits:
            memory: "4Gi"
            cpu: "1000m"
        # Проверки работоспособности
        livenessProbe:
          httpGet:
            path: /
            port: 8000
          initialDelaySeconds: 60
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 5